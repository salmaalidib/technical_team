/// إعدادات مشتركة لكل جداول SfDataGrid في التطبيق.
class DataTableConfigs {
  DataTableConfigs._();

  /// خيارات عدد الصفوف لكل صفحة في الـ pager.
  static const List<int> pageSizes = [10, 20, 50];
}
